349 results - 42 files

tests\dreamstate\ab-emotion-parity.test.ts:
  4  // Why: Prevents emotional drift between UI/UX variants
  5: // How: Uses canonical mocks and asserts Codex-aligned emotional parity
  6  
  7: import { createEmotionalPayload } from '../mocks/dreamstate-core';
  8  import { describe, it, expect } from '@jest/globals';

tests\dreamstate\agent-workflow-sequencing.test.ts:
   4  // Why: Ensures correct order and emotional output in multi-agent workflows
   5: // How: Uses canonical mocks and asserts Codex-aligned agent sequencing
   6  
   7: import { mockAgentWorkflow, requireMock } from '../mocks/dreamstate-core';
   8  

  13      // How: Compare agent sequence and outputs
  14:     if (!mockAgentWorkflow) requireMock('mockAgentWorkflow');
  15      const expectedOrder = ['Parser', 'Generator', 'Validator'];
  16:     mockAgentWorkflow.forEach((step, idx) => {
  17        expect(step.agent).toBe(expectedOrder[idx]);

tests\dreamstate\chaos-agent-outage.test.ts:
   4  // Why: Ensures system resilience and fallback logic under agent failure
   5: // How: Uses canonical mocks and asserts Codex-aligned chaos/fallback handling
   6  
   7: import { mockAgentWorkflow, requireMock } from '../mocks/dreamstate-core';
   8  

  13      // How: Remove an agent and assert fallback is triggered
  14:     if (!mockAgentWorkflow) requireMock('mockAgentWorkflow');
  15:     const partialWorkflow = mockAgentWorkflow.slice(0, 2); // Remove Validator
  16:     const fallbackTriggered = partialWorkflow.length < mockAgentWorkflow.length;
  17      expect(fallbackTriggered).toBe(true);

tests\dreamstate\chaos-disk-failure.test.ts:
   4  // Why: Ensures system resilience and fallback logic under storage failure
   5: // How: Uses canonical mocks and asserts Codex-aligned chaos/recovery handling
   6  
   7: import { mockEmotionalPayload, requireMock } from '../mocks/dreamstate-core';
   8  

  13      // How: Set a diskFailure flag and assert recovery is triggered
  14:     if (!mockEmotionalPayload) requireMock('mockEmotionalPayload');
  15      const diskFailure = true;
  16:     const recoveryTriggered = diskFailure && mockEmotionalPayload.trustScore > 0.9;
  17      expect(recoveryTriggered).toBe(true);

tests\dreamstate\chaos-emotional-drift.test.ts:
  4  // Why: Ensures emotional UX resilience under chaos
  5: // How: Uses canonical mocks and asserts Codex-aligned chaos testing
  6  
  7: import { createEmotionalPayload } from '../mocks/dreamstate-core';
  8  

tests\dreamstate\chaos-network-failure.test.ts:
   4  // Why: Ensures emotional continuity and fallback purity under network chaos
   5: // How: Uses canonical mocks and asserts Codex-aligned chaos testing
   6  
   7: import { mockChaosNetworkFailure, requireMock } from '../mocks/dreamstate-core';
   8  

  13      // How: Check that recovery is false and affected agents are correct
  14:     if (!mockChaosNetworkFailure) requireMock('mockChaosNetworkFailure');
  15:     expect(mockChaosNetworkFailure.event).toBe('network-partition');
  16:     expect(Array.isArray(mockChaosNetworkFailure.affectedAgents)).toBe(true);
  17:     expect(mockChaosNetworkFailure.recovery).toBe(false);
  18    });

tests\dreamstate\decay-prevention-suite.test.ts:
   4  // Why: Prevents silent emotional decay and regression after updates
   5: // How: Uses canonical mocks and asserts Codex-aligned emotional output
   6  
   7: import { mockEmotionalPayload, requireMock } from '../mocks/dreamstate-core';
   8  import { describe, it, expect } from '@jest/globals';

  14      // How: Mutate trustScore and assert detection logic
  15:     if (!mockEmotionalPayload) requireMock('mockEmotionalPayload');
  16:     const revisedPayload = { ...mockEmotionalPayload, trustScore: 0.2 };
  17      expect(revisedPayload.trustScore).toBeLessThan(0.5);

  23      // How: Change payload and assert mismatch
  24:     const regressedPayload = { ...mockEmotionalPayload, payload: 'Unsupported response.' };
  25:     expect(regressedPayload.payload).not.toBe(mockEmotionalPayload.payload);
  26    });

  31      // How: Change intent hash and assert drift
  32:     const driftedPayload = { ...mockEmotionalPayload, emotionIntentHash: 'intent-drifted' };
  33:     expect(driftedPayload.emotionIntentHash).not.toBe(mockEmotionalPayload.emotionIntentHash);
  34    });

tests\dreamstate\emotional-spectrum-coverage.test.ts:
   4  // Why: Guarantees full emotional spectrum is covered and validated
   5: // How: Uses canonical mocks and asserts Codex-aligned emotional output
   6  
   7: import { mockEmotionalPayload, requireMock } from '../mocks/dreamstate-core';
   8  

  13      // How: Check for presence of key emotional tones
  14:     if (!mockEmotionalPayload) requireMock('mockEmotionalPayload');
  15      const emotionalSpectrum = ['empathy', 'excitement', 'reassuring', 'neutral', 'anxious'];
  16:     const payloads = emotionalSpectrum.map(tone => ({ ...mockEmotionalPayload, tone }));
  17      emotionalSpectrum.forEach((tone, idx) => {

tests\dreamstate\emotional-ux-core.test.ts:
   4  // Why: Ensures emotional intent, trustScore, and tone are preserved and protected from drift or misclassification
   5: // How: Uses canonical mocks and asserts Codex-aligned emotional output
   6  
   7: import { createEmotionalPayload } from '../mocks/dreamstate-core'; // @codex-temp-real-input
   8  import { describe, it, expect } from '@jest/globals';

  13      // Why: Ensures emotional intent and tone are not drifting
  14:     // How: Use canonical mock and assert expected tone and intent hash
  15      const payload = createEmotionalPayload();

  22      // Why: Prevents conflicting emotional signals in multi-agent scenarios
  23:     // How: Create a mock collision and assert Codex override
  24      const agents = [

  51      // Why: Ensures sarcasm is not misclassified as supportive
  52:     // How: Use a mock sarcastic payload and assert rejection
  53      const payload = createEmotionalPayload();

tests\dreamstate\fallback-cascade-integrity.test.ts:
   4  // Why: Ensures fallback chains are robust and non-leaky
   5: // How: Uses canonical mocks and asserts Codex-aligned fallback cascade logic
   6  
   7: import { mockFallbackChain, requireMock } from '../mocks/dreamstate-core';
   8  

  13      // How: Check that fallback chain ends with a success and no skipped steps
  14:     if (!mockFallbackChain) requireMock('mockFallbackChain');
  15:     const lastStep = mockFallbackChain[mockFallbackChain.length - 1];
  16      expect(lastStep.status).toBe('success');
  17:     mockFallbackChain.forEach((step, idx) => {
  18        if (idx > 0) {
  19:         expect(step.step).toBe(mockFallbackChain[idx - 1].step + 1);
  20        }

tests\dreamstate\fallback-contamination-sandbox.test.ts:
   4  // Why: Ensures fallback logic does not contaminate unrelated workflows
   5: // How: Uses canonical mocks and asserts Codex-aligned fallback isolation
   6  
   7: import { mockFallbackChain, requireMock } from '../mocks/dreamstate-core';
   8  

  13      // How: Simulate two chains and assert no cross-contamination
  14:     if (!mockFallbackChain) requireMock('mockFallbackChain');
  15      const unrelatedChain = [

  17      ];
  18:     // Ensure no agent from unrelatedChain appears in mockFallbackChain
  19      unrelatedChain.forEach(unrelated => {
  20:       mockFallbackChain.forEach(fallback => {
  21          expect(fallback.agent).not.toBe(unrelated.agent);

tests\dreamstate\fallback-cross-talk.test.ts:
   4  // Why: Ensures fallback isolation and emotional integrity
   5: // How: Uses canonical mocks and asserts Codex-aligned fallback logic
   6  
   7: import { mockFallbackChain, requireMock } from '../mocks/dreamstate-core';
   8  import { describe, it, expect } from '@jest/globals';

  14      // How: Assert that only one agent is active per fallback step
  15:     if (!mockFallbackChain) requireMock('mockFallbackChain');
  16:     const activeAgents = mockFallbackChain.map(step => step.agent);
  17      const uniqueAgents = new Set(activeAgents);

tests\dreamstate\fallback-depth-limit.test.ts:
   4  // Why: Prevents runaway fallback chains and resource exhaustion
   5: // How: Uses canonical mocks and asserts Codex-aligned fallback depth enforcement
   6  
   7: import { mockFallbackChain, requireMock } from '../mocks/dreamstate-core';
   8  

  13      // How: Create a chain of 8 and assert enforcement at 7
  14:     if (!mockFallbackChain) requireMock('mockFallbackChain');
  15      const deepChain = Array.from({ length: 8 }, (_, i) => ({ step: i + 1, agent: `Agent${i + 1}`, status: 'fail' }));

tests\dreamstate\fallback-nesting-integrity.test.ts:
   4  // Why: Ensures fallback logic is robust and non-leaky
   5: // How: Uses canonical mocks and asserts Codex-aligned fallback nesting
   6  
   7: import { mockFallbackChain, requireMock } from '../mocks/dreamstate-core';
   8  import { describe, it, expect } from '@jest/globals';

  14      // How: Assert that each fallback only triggers the next if it fails
  15:     if (!mockFallbackChain) requireMock('mockFallbackChain');
  16      let triggered = false;
  17:     for (let i = 0; i < mockFallbackChain.length; i++) {
  18:       const step = mockFallbackChain[i];
  19        if (step.status === 'fail') {

  21          // Next fallback must exist
  22:         if (i + 1 < mockFallbackChain.length) {
  23:           expect(mockFallbackChain[i + 1].status).toBeDefined();
  24          }

  27      // Final fallback must succeed
  28:     const lastStep = mockFallbackChain[mockFallbackChain.length - 1];
  29      expect(lastStep.status).toBe('success');

tests\dreamstate\golden-emotion-snapshot.test.ts:
   4  // Why: Prevents emotional drift and ensures snapshot authenticity
   5: // How: Uses canonical mocks and asserts Codex-aligned snapshot integrity
   6  
   7: import { createEmotionalPayload } from '../mocks/dreamstate-core';
   8  

  12      // Why: Prevents emotional drift and ensures snapshot authenticity
  13:     // How: Use a mock signature and assert integrity
  14      const payloadString = JSON.stringify(createEmotionalPayload());

tests\dreamstate\locale-translation-accuracy.test.ts:
   4  // Why: Ensures emotional and semantic parity in internationalized UX
   5: // How: Uses canonical mocks and asserts Codex-aligned translation accuracy
   6  
   7: import { mockEmotionalPayload, requireMock } from '../mocks/dreamstate-core';
   8  

  13      // How: Compare payloads with different locales and assert tone/intent
  14:     if (!mockEmotionalPayload) requireMock('mockEmotionalPayload');
  15      const locales = ['en-US', 'de-DE', 'ja-JP'];
  16:     const payloads = locales.map(locale => ({ ...mockEmotionalPayload, locale }));
  17      payloads.forEach(payload => {
  18:       expect(payload.tone).toBe(mockEmotionalPayload.tone);
  19:       expect(payload.payload).toBe(mockEmotionalPayload.payload);
  20      });

tests\dreamstate\multi-locale-tone-parity.test.ts:
   4  // Why: Ensures emotional intent is preserved in all locales
   5: // How: Uses canonical mocks and asserts Codex-aligned tone parity
   6  
   7: import { mockEmotionalPayload, requireMock } from '../mocks/dreamstate-core';
   8  

  13      // How: Compare tone field across locales
  14:     if (!mockEmotionalPayload) requireMock('mockEmotionalPayload');
  15      const locales = ['en-US', 'fr-FR', 'es-ES'];
  16:     const payloads = locales.map(locale => ({ ...mockEmotionalPayload, locale }));
  17:     const baseTone = mockEmotionalPayload.tone;
  18      payloads.forEach(payload => {

tests\dreamstate\open-telemetry-span-gap.test.ts:
   4  // Why: Ensures trace continuity and observability
   5: // How: Uses canonical mocks and asserts Codex-aligned span propagation
   6  
   7: import { mockEmotionalPayload, requireMock } from '../mocks/dreamstate-core';
   8  

  13      // How: Simulate span chain and check for continuity
  14:     if (!mockEmotionalPayload) requireMock('mockEmotionalPayload');
  15      const spans = [

tests\dreamstate\performance-baseline.test.ts:
   4  // Why: Ensures system meets minimum performance and trust requirements
   5: // How: Uses canonical mocks and asserts Codex-aligned performance metrics
   6  
   7: import { mockEmotionalPayload, requireMock } from '../mocks/dreamstate-core';
   8  

  12      // Why: Ensures system meets minimum performance and trust requirements
  13:     // How: Use mock trustScore and assert above threshold
  14:     if (!mockEmotionalPayload) requireMock('mockEmotionalPayload');
  15      const minTrustScore = 0.9;
  16:     expect(mockEmotionalPayload.trustScore).toBeGreaterThanOrEqual(minTrustScore);
  17    });

tests\dreamstate\prompt-forward-compat.test.ts:
   4  // Why: Prevents prompt breakage on schema evolution
   5: // How: Uses canonical mocks and asserts Codex-aligned forward compatibility
   6  
   7: import { mockEmotionalPayload, requireMock } from '../mocks/dreamstate-core';
   8  

  13      // How: Add a new field and assert prompt still processes
  14:     if (!mockEmotionalPayload) requireMock('mockEmotionalPayload');
  15:     const evolvedPayload = { ...mockEmotionalPayload, newField: 'future-proof' };
  16:     expect(evolvedPayload.tone).toBe(mockEmotionalPayload.tone);
  17      expect(evolvedPayload.newField).toBe('future-proof');

tests\dreamstate\rate-limit-message-wrapper.test.ts:
   4  // Why: Prevents user trust erosion during rate limiting
   5: // How: Uses canonical mocks and asserts Codex-aligned emotional messaging
   6  
   7: import { mockEmotionalPayload, requireMock } from '../mocks/dreamstate-core';
   8  import { describe, it, expect } from '@jest/globals';

  14      // How: Assert that the emotional tone and trustScore are Codex-aligned
  15:     if (!mockEmotionalPayload) requireMock('mockEmotionalPayload');
  16:     const rateLimitMessage = { ...mockEmotionalPayload, payload: 'You are being rate limited. Please try again soon.' };
  17      expect(rateLimitMessage.tone).toBe('reassuring');

tests\dreamstate\README.md:
   8  - Guarantees coverage for emotional drift, schema migration, agent workflows, fallback chains, chaos scenarios, and trustScore logic.
   9: - Centralizes mocks and edge cases for full-system validation.
  10  

  17  - All tests are required; no bypass or modification is allowed without Codex Override.
  18: - Mocks and edge cases are centralized in `/tests/mocks/dreamstate-core.ts`.
  19  - All failures, chaos events, and regressions are logged and reflected in `/cursor/auto-actions.log.md` for audit and recovery.

  21  **Codex Safeguard:**
  22: If any required test or mock is missing, Sentinel auto-generates scaffolds using canonical templates. All test failures and regressions are logged for audit and Codex review. 

tests\dreamstate\sarcasm-tone-misclassify.test.ts:
   4  // Why: Ensures emotional intent is not misclassified as supportive
   5: // How: Uses canonical mocks and asserts Codex-aligned sarcasm rejection
   6  
   7: import { mockEmotionalPayload, requireMock } from '../mocks/dreamstate-core';
   8  import { describe, it, expect } from '@jest/globals';

  13      // Why: Ensures emotional intent is not misclassified as supportive
  14:     // How: Use a mock sarcastic payload and assert Codex-aligned rejection
  15:     if (!mockEmotionalPayload) requireMock('mockEmotionalPayload');
  16:     const sarcasticPayload = { ...mockEmotionalPayload, tone: 'sarcastic', payload: 'Oh, great job...' };
  17      const isSupportive = sarcasticPayload.tone === 'reassuring' && !/sarcasm|irony/i.test(sarcasticPayload.payload);

tests\dreamstate\schema-backward-compat.test.ts:
   4  // Why: Ensures backward compatibility and resilience
   5: // How: Uses canonical mocks and asserts Codex-aligned backward compatibility
   6  
   7: import { mockEmotionalPayload, requireMock } from '../mocks/dreamstate-core';
   8  

  13      // How: Remove a field and assert system still processes
  14:     if (!mockEmotionalPayload) requireMock('mockEmotionalPayload');
  15      // Simulate legacy schema by omitting emotionIntentHash
  16:     const { emotionIntentHash, ...legacyPayload } = mockEmotionalPayload;
  17      expect(legacyPayload.tone).toBeDefined();

tests\dreamstate\schema-migration-emotion.test.ts:
   4  // Why: Prevents loss of emotional fidelity during schema migrations
   5: // How: Uses canonical mocks and asserts Codex-aligned schema migration
   6  
   7: import { mockEmotionalPayload, requireMock } from '../mocks/dreamstate-core';
   8  import { describe, it, expect } from '@jest/globals';

  14      // How: Copy payload, simulate upgrade, and assert metadata unchanged
  15:     if (!mockEmotionalPayload) requireMock('mockEmotionalPayload');
  16:     const upgradedPayload = { ...mockEmotionalPayload, schemaVersion: 2 };
  17:     expect(upgradedPayload.tone).toBe(mockEmotionalPayload.tone);
  18:     expect(upgradedPayload.emotionIntentHash).toBe(mockEmotionalPayload.emotionIntentHash);
  19    });

tests\dreamstate\security-input-sanitization.test.ts:
   4  // Why: Ensures system integrity and emotional intent preservation
   5: // How: Uses canonical mocks and asserts Codex-aligned sanitization
   6  
   7: import { mockMaliciousInput, requireMock } from '../mocks/dreamstate-core';
   8  

  13      // How: Replace dangerous characters and assert intent is preserved
  14:     if (!mockMaliciousInput) requireMock('mockMaliciousInput');
  15:     const sanitized = { ...mockMaliciousInput, userPrompt: mockMaliciousInput.userPrompt.replace(/[^a-zA-Z0-9 .,?!]/g, '') };
  16      expect(sanitized.userPrompt).not.toMatch(/;|--|\*/);
  17:     expect(sanitized.locale).toBe(mockMaliciousInput.locale);
  18    });

tests\dreamstate\snapshot-approval-gate.test.ts:
   4  // Why: Prevents unauthorized or unreviewed emotional drift
   5: // How: Uses canonical mocks and asserts Codex-aligned approval logic
   6  
   7: import { mockEmotionalPayload, requireMock } from '../mocks/dreamstate-core';
   8  

  12      // Why: Prevents unauthorized or unreviewed emotional drift
  13:     // How: Use a mock approval flag and assert enforcement
  14:     if (!mockEmotionalPayload) requireMock('mockEmotionalPayload');
  15      const approvalRequired = true;
  16:     const approved = approvalRequired && mockEmotionalPayload.trustScore > 0.9;
  17      expect(approved).toBe(true);

tests\dreamstate\snapshot-duplicate-race.test.ts:
   4  // Why: Ensures snapshot consistency and prevents duplication
   5: // How: Uses canonical mocks and asserts Codex-aligned race prevention
   6  
   7: import { mockEmotionalPayload, requireMock } from '../mocks/dreamstate-core';
   8  

  13      // How: Use a Set to simulate snapshot IDs and concurrent creation
  14:     if (!mockEmotionalPayload) requireMock('mockEmotionalPayload');
  15      const concurrentSnapshots = [
  16:       { ...mockEmotionalPayload, traceId: 'trace-1' },
  17:       { ...mockEmotionalPayload, traceId: 'trace-1' }, // duplicate
  18:       { ...mockEmotionalPayload, traceId: 'trace-2' }
  19      ];

tests\dreamstate\snapshot-key-rotation.test.ts:
   4  // Why: Ensures cryptographic integrity of emotional snapshots
   5: // How: Uses canonical mocks and asserts Codex-aligned signature and rollover
   6  
   7: import { mockEmotionalPayload, requireMock } from '../mocks/dreamstate-core';
   8  import { describe, it, expect } from '@jest/globals';

  13      // Why: Ensures cryptographic integrity of emotional snapshots
  14:     // How: Use a mock hash function and assert signature matches expected
  15:     if (!mockEmotionalPayload) requireMock('mockEmotionalPayload');
  16:     const payloadString = JSON.stringify(mockEmotionalPayload);
  17      // Simulate SHA-256 hash (deterministic for test)

  26      // Why: Ensures new signatures are unique and old ones are invalidated
  27:     // How: Use a mock key and assert signature changes after rollover
  28:     if (!mockEmotionalPayload) requireMock('mockEmotionalPayload');
  29:     const payloadString = JSON.stringify(mockEmotionalPayload);
  30      const fakeSHA256 = (input: string, key: string) => 'sha256-' + Buffer.from(input + key).toString('base64').slice(0, 8);

tests\dreamstate\system-resilience-core.test.ts:
   4  // Why: Ensures system can recover from failures while preserving emotional UX
   5: // How: Uses canonical mocks and asserts Codex-aligned fallback and recovery
   6  
   7: import { buildFallbackChain, createEmotionalPayload } from '../mocks/dreamstate-core'; // @codex-temp-real-input
   8  import { describe, it, expect } from '@jest/globals';

  22      // Why: Ensures emotional UX is not lost during fallback
  23:     // How: Use fallback chain and mock payload
  24      const recoveryPayload = { ...createEmotionalPayload(), recovery: true };

tests\dreamstate\traceid-continuity.test.ts:
   4  // Why: Ensures trace continuity for audit and recovery
   5: // How: Uses canonical mocks and asserts Codex-aligned traceId logic
   6  
   7: import { mockEmotionalPayload, requireMock } from '../mocks/dreamstate-core';
   8  import { describe, it, expect } from '@jest/globals';

  13      // Why: Ensures traceId is preserved across agent hops
  14:     // How: Use canonical mock and agent workflow
  15:     if (!mockEmotionalPayload) requireMock('mockEmotionalPayload');
  16      const agentTraceIds = ['trace-123', 'trace-123', 'trace-123'];

  22      // Why: Ensures traceId is not lost during fallback
  23:     // How: Use fallback chain and mock payload
  24      const fallbackTraceIds = ['trace-123', 'trace-123'];

tests\dreamstate\traceid-failure-recovery.test.ts:
   4  // Why: Guarantees trace continuity under failure conditions
   5: // How: Uses canonical mocks and asserts Codex-aligned traceId recovery
   6  
   7: import { mockEmotionalPayload, requireMock } from '../mocks/dreamstate-core';
   8  

  13      // How: Remove sessionId and assert traceId remains
  14:     if (!mockEmotionalPayload) requireMock('mockEmotionalPayload');
  15      // Simulate legacy/failure by omitting sessionId
  16:     const { sessionId, ...failedPayload } = mockEmotionalPayload;
  17      expect(failedPayload.traceId).toBeDefined();

tests\dreamstate\trust-restore-post-coldstart.test.ts:
   4  // Why: Ensures trustScore is resilient to system restarts
   5: // How: Uses canonical mocks and asserts Codex-aligned trust restoration
   6  
   7: import { mockEmotionalPayload, requireMock } from '../mocks/dreamstate-core';
   8  

  13      // How: Reset trustScore and assert restoration logic
  14:     if (!mockEmotionalPayload) requireMock('mockEmotionalPayload');
  15:     const coldStartPayload = { ...mockEmotionalPayload, trustScore: 0 };
  16:     // Simulate restoration logic (mock)
  17:     const restoredPayload = { ...coldStartPayload, trustScore: mockEmotionalPayload.trustScore };
  18      expect(restoredPayload.trustScore).toBeGreaterThan(0.9);

tests\dreamstate\trustscore-unrecoverable-drop.test.ts:
   4  // Why: Ensures system logs, blocks, and recovers from trustScore failures
   5: // How: Uses canonical mocks and asserts Codex-aligned trustScore drop handling
   6  
   7: import { mockEmotionalPayload, requireMock } from '../mocks/dreamstate-core';
   8  

  13      // How: Set trustScore to 0 and assert detection
  14:     if (!mockEmotionalPayload) requireMock('mockEmotionalPayload');
  15:     const droppedPayload = { ...mockEmotionalPayload, trustScore: 0 };
  16      expect(droppedPayload.trustScore).toBe(0);

tests\dreamstate\api\add-client-emotional-fallback.test.ts:
  11  import handler from '../../../api/add_client';
  12: // @ts-expect-error: node-mocks-http types must be installed as a dev dependency
  13: import { createMocks } from 'node-mocks-http';
  14  

  19    it('returns success for valid input and logs action', async () => {
  20:     const { req, res } = createMocks({
  21        method: 'POST',

  23      });
  24:     // Mock Airtable API call here if needed
  25      await handler(req, res);

  30    it('returns validation error for missing email and triggers fallback', async () => {
  31:     const { req, res } = createMocks({
  32        method: 'POST',

  41    it('handles Airtable API failure with emotional fallback and logs', async () => {
  42:     const { req, res } = createMocks({
  43        method: 'POST',

  45      });
  46:     // Simulate Airtable API failure (mock axios)
  47:     // TODO: Mock axios to throw
  48      await handler(req, res);

tests\dreamstate\api\add-project-emotional-fallback.test.ts:
  11  import handler from '../../../api/add_project';
  12: // @ts-expect-error: node-mocks-http types must be installed as a dev dependency
  13: import { createMocks } from 'node-mocks-http';
  14  

  19    it('returns success for valid input and logs action', async () => {
  20:     const { req, res } = createMocks({
  21        method: 'POST',

  23      });
  24:     // Mock Airtable API call here if needed
  25      await handler(req, res);

  30    it('returns validation error for missing promptType/input and triggers fallback', async () => {
  31:     const { req, res } = createMocks({
  32        method: 'POST',

  41    it('handles Airtable API failure with emotional fallback and logs', async () => {
  42:     const { req, res } = createMocks({
  43        method: 'POST',

  45      });
  46:     // Simulate Airtable API failure (mock axios)
  47:     // TODO: Mock axios to throw
  48      await handler(req, res);

tests\dreamstate\api\admin-status-emotional-fallback.test.ts:
  11  import handler from '../../../api/internal/admin_status';
  12: // @ts-expect-error: node-mocks-http types must be installed as a dev dependency
  13: import { createMocks } from 'node-mocks-http';
  14  

  16    it('returns success for GET and logs action', async () => {
  17:     const { req, res } = createMocks({ method: 'GET' });
  18      await handler(req, res);

  23    it('returns method not allowed for non-GET and triggers fallback', async () => {
  24:     const { req, res } = createMocks({ method: 'POST' });
  25      await handler(req, res);

  31    it('handles processing failure with emotional fallback and logs', async () => {
  32:     const { req, res } = createMocks({ method: 'GET' });
  33:     // TODO: Mock handler internals to throw
  34      await handler(req, res);
  35:     // This would only be testable if handler internals are mockable
  36      // expect(res._getStatusCode()).toBe(500);

tests\dreamstate\api\openai-handler-emotional-fallback.test.ts:
  11  import handler from '../../../api/openaiHandler';
  12: // @ts-expect-error: node-mocks-http types must be installed as a dev dependency
  13: import { createMocks } from 'node-mocks-http';
  14  

  16    it('returns success for valid input and logs action', async () => {
  17:     const { req, res } = createMocks({
  18        method: 'POST',

  20      });
  21:     // Mock OpenAI API call here if needed
  22      await handler(req, res);

  27    it('returns validation error for missing promptType/input and triggers fallback', async () => {
  28:     const { req, res } = createMocks({
  29        method: 'POST',

  38    it('handles OpenAI API failure with emotional fallback and logs', async () => {
  39:     const { req, res } = createMocks({
  40        method: 'POST',

  42      });
  43:     // Simulate OpenAI API failure (mock openai.chat.completions.create)
  44:     // TODO: Mock OpenAI to throw
  45      await handler(req, res);

tests\dreamstate\api\prompt-handler-emotional-fallback.test.ts:
  11  import handler from '../../../api/prompt_handler';
  12: // @ts-expect-error: node-mocks-http types must be installed as a dev dependency
  13: import { createMocks } from 'node-mocks-http';
  14  

  16    it('returns success for valid input and logs action', async () => {
  17:     const { req, res } = createMocks({
  18        method: 'POST',

  20      });
  21:     // Mock OpenAI API call here if needed
  22      await handler(req, res);

  27    it('returns validation error for missing promptType/input and triggers fallback', async () => {
  28:     const { req, res } = createMocks({
  29        method: 'POST',

  38    it('returns enforcement breach and triggers fallback', async () => {
  39:     const { req, res } = createMocks({
  40        method: 'POST',

  42      });
  43:     // TODO: Mock enforceChecklistStatusGuard to return blocked
  44      await handler(req, res);

  50    it('handles OpenAI API failure with emotional fallback and logs', async () => {
  51:     const { req, res } = createMocks({
  52        method: 'POST',

  54      });
  55:     // Simulate OpenAI API failure (mock openai.createChatCompletion)
  56:     // TODO: Mock OpenAI to throw
  57      await handler(req, res);

tests\dreamstate\api\selfcheck-api-emotional-fallback.test.ts:
  11  import handler from '../../../api/devtools/selfcheck-api';
  12: // @ts-expect-error: node-mocks-http types must be installed as a dev dependency
  13: import { createMocks } from 'node-mocks-http';
  14  

  16    it('returns success for healthy selfcheck and logs action', async () => {
  17:     const { req, res } = createMocks({ method: 'GET' });
  18      await handler(req, res);

  23    it('returns error for failed selfcheck and triggers fallback', async () => {
  24:     const { req, res } = createMocks({ method: 'GET' });
  25:     // TODO: Mock handler internals to simulate failure
  26      await handler(req, res);
  27:     // This would only be testable if handler internals are mockable
  28      // expect(res._getStatusCode()).toBe(500);

tests\dreamstate\api\stripe-webhook-emotional-fallback.test.ts:
  11  import handler from '../../../api/stripe_webhook';
  12: // @ts-expect-error: node-mocks-http types must be installed as a dev dependency
  13: import { createMocks } from 'node-mocks-http';
  14  

  16    it('returns success for valid signature and event, logs action', async () => {
  17:     const { req, res } = createMocks({
  18        method: 'POST',

  21      });
  22:     // TODO: Mock verifySignature and event validation
  23      await handler(req, res);

  28    it('returns error for invalid signature and triggers fallback', async () => {
  29:     const { req, res } = createMocks({
  30        method: 'POST',

  33      });
  34:     // TODO: Mock verifySignature to throw
  35      await handler(req, res);

  41    it('returns error for invalid event and triggers fallback', async () => {
  42:     const { req, res } = createMocks({
  43        method: 'POST',

  46      });
  47:     // TODO: Mock event validation to throw
  48      await handler(req, res);

tests\dreamstate\api\webhook-health-emotional-fallback.test.ts:
  11  import handler from '../../../api/webhook_health';
  12: // @ts-expect-error: node-mocks-http types must be installed as a dev dependency
  13: import { createMocks } from 'node-mocks-http';
  14  

  16    it('returns success for GET and logs action', async () => {
  17:     const { req, res } = createMocks({ method: 'GET' });
  18      await handler(req, res);

  23    it('returns method not allowed for non-GET and triggers fallback', async () => {
  24:     const { req, res } = createMocks({ method: 'POST' });
  25      await handler(req, res);

  31    it('handles processing failure with emotional fallback and logs', async () => {
  32:     const { req, res } = createMocks({ method: 'GET' });
  33:     // TODO: Mock handler internals to throw
  34      await handler(req, res);
  35:     // This would only be testable if handler internals are mockable
  36      // expect(res._getStatusCode()).toBe(500);

[
  {
    "fieldName": "recordId",
    "fieldType": "ulid",
    "required": true,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Primary key for ReferralTriggers.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "createdAt",
    "fieldType": "timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Used for audit and recovery.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "updatedAt",
    "fieldType": "timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Tracks last update for audit.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "sourceUserId",
    "fieldType": "string",
    "required": true,
    "default": null,
    "emotionalRole": "trust",
    "dataSensitivity": "pii",
    "contextScope": "session",
    "orchestrationNotes": "User initiating the referral.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "targetUserId",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "growth",
    "dataSensitivity": "pii",
    "contextScope": "session",
    "orchestrationNotes": "User being referred (may be null if not yet registered).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to null if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "triggerType",
    "fieldType": "string",
    "required": true,
    "default": "invite",
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Type of referral trigger (e.g., invite, share, reward, viralLoop).",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to 'invite' if missing and log fallback event.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "triggerContext",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Contextual metadata for the trigger (e.g., campaign, channel, message).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "trustSignal",
    "fieldType": "string",
    "required": false,
    "default": "neutral",
    "emotionalRole": "trust",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Signal of trust level (e.g., high, medium, low, neutral).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 'neutral' if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "emotionalTag",
    "fieldType": "string",
    "required": false,
    "default": "",
    "emotionalRole": "emotion",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Emotional context or tag for the referral (e.g., gratitude, excitement, curiosity).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow empty string if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "referralOutcome",
    "fieldType": "string",
    "required": false,
    "default": "pending",
    "emotionalRole": "outcome",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Outcome of the referral (e.g., pending, accepted, declined, expired).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 'pending' if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "fallbackUsed",
    "fieldType": "boolean",
    "required": false,
    "default": false,
    "emotionalRole": "resilience",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Indicates if fallback logic was triggered for this referral.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to false if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "auditTrail",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Audit log for all changes and fallback events.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing.",
      "auditTrail": true
    }
  }
] 
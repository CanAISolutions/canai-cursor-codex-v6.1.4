[
  {
    "fieldName": "statId",
    "fieldType": "ULID",
    "required": true,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Primary key for PromptPerformanceStats records.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "createdAt",
    "fieldType": "Timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Used for audit and recovery.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "updatedAt",
    "fieldType": "Timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Tracks last update for audit.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "promptId",
    "fieldType": "ULID",
    "required": true,
    "default": null,
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Foreign key to PromptLogs.recordId for performance linkage.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "promptType",
    "fieldType": "string",
    "required": true,
    "default": null,
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "meta",
    "orchestrationNotes": "Type of prompt (e.g., businessPlan, socialContent, siteAudit).",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "executionTimeMs",
    "fieldType": "number",
    "required": false,
    "default": null,
    "emotionalRole": "performance",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Execution time in milliseconds for the prompt.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not measured. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "tokenCount",
    "fieldType": "number",
    "required": false,
    "default": null,
    "emotionalRole": "efficiency",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Number of tokens used in the prompt execution.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not measured. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "costUSD",
    "fieldType": "number",
    "required": false,
    "default": 0.0,
    "emotionalRole": "cost",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Cost in USD for the prompt execution.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 0.0 if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "successFlag",
    "fieldType": "boolean",
    "required": false,
    "default": true,
    "emotionalRole": "outcome",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Flag indicating if the prompt execution was successful.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to true if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "errorType",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Type of error if execution failed (e.g., timeout, validation, fallback).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if no error. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "resonanceScore",
    "fieldType": "number",
    "required": false,
    "default": null,
    "emotionalRole": "empathy",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Emotional resonance score (0.0–1.0) for the prompt output.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not measured. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "clarityIndex",
    "fieldType": "number",
    "required": false,
    "default": null,
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Clarity score (0–10) for the prompt output.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not measured. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "auditTrail",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Audit log for all changes and fallback events.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing. Audit trail required.",
      "auditTrail": true
    }
  }
] 
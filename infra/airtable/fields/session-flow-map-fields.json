[
  {
    "fieldName": "flowId",
    "fieldType": "ULID",
    "required": true,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Primary key for SessionFlowMap records.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "sessionId",
    "fieldType": "string",
    "required": true,
    "default": null,
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Foreign key to SessionAnalytics.sessionId for flow mapping.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "userId",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "pii",
    "contextScope": "user",
    "orchestrationNotes": "User identifier for flow personalization and analytics.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if system-generated. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "flowType",
    "fieldType": "string",
    "required": true,
    "default": "default",
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Type of flow (e.g., onboarding, recovery, feedback, mainline).",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to 'default' if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "flowStart",
    "fieldType": "Timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Timestamp for when the flow started.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "flowEnd",
    "fieldType": "Timestamp",
    "required": false,
    "default": null,
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Timestamp for when the flow ended.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if flow is ongoing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "flowStatus",
    "fieldType": "string",
    "required": false,
    "default": "active",
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Status of the flow (e.g., active, completed, abandoned, fallback).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 'active' if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "steps",
    "fieldType": "array",
    "required": false,
    "default": [],
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Ordered list of step objects or IDs representing the flow sequence.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty array if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "fallbackTriggered",
    "fieldType": "boolean",
    "required": false,
    "default": false,
    "emotionalRole": "resilience",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Indicates if fallback logic was triggered during the flow.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to false if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "emotionalDropDetected",
    "fieldType": "boolean",
    "required": false,
    "default": false,
    "emotionalRole": "emotion",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Flag if an emotional drop or disengagement was detected in the flow.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to false if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "feedbackLoop",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "feedback",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Object capturing feedback events, scores, or triggers within the flow.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "auditTrail",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Audit log for all changes and fallback events.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing. Audit trail required.",
      "auditTrail": true
    }
  }
] 
[
  {
    "fieldName": "recordId",
    "fieldType": "ulid",
    "required": true,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Primary key for SessionAnalytics.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "createdAt",
    "fieldType": "timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Used for audit and recovery.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "updatedAt",
    "fieldType": "timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Tracks last update for audit.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "promptLogLink",
    "fieldType": "ulid",
    "required": true,
    "default": null,
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Foreign key to PromptLogs.recordId for orchestration.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "feedbackLogLink",
    "fieldType": "ulid",
    "required": false,
    "default": null,
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Foreign key to FeedbackLogs.recordId for orchestration.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to null if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "sessionId",
    "fieldType": "string",
    "required": true,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Unique session identifier.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "momentumScore",
    "fieldType": "number",
    "required": true,
    "default": 0.5,
    "emotionalRole": "momentum",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Session progress velocity score.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to 0.5 if missing and log fallback event.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "frictionScore",
    "fieldType": "number",
    "required": true,
    "default": 0.5,
    "emotionalRole": "friction",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Session friction or resistance score.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to 0.5 if missing and log fallback event.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "clarityIndex",
    "fieldType": "number",
    "required": true,
    "default": 5,
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Session clarity score (0–10).",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to 5 if missing and log fallback event.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "resonanceScore",
    "fieldType": "number",
    "required": true,
    "default": 0.5,
    "emotionalRole": "empathy",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Session emotional resonance score (0.0–1.0).",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to 0.5 if missing and log fallback event.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "emotionTensor",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "emotion",
    "dataSensitivity": "pii",
    "contextScope": "session",
    "orchestrationNotes": "Multi-dimensional emotion state for the session.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "emotionTrendScore",
    "fieldType": "number",
    "required": false,
    "default": 0.0,
    "emotionalRole": "emotion",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Session emotional trajectory score.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 0.0 if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "sessionOutcome",
    "fieldType": "string",
    "required": false,
    "default": "unknown",
    "emotionalRole": "outcome",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Session outcome (e.g., success, fallback, recovery, abandoned).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 'unknown' if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "recoveryTriggered",
    "fieldType": "boolean",
    "required": false,
    "default": false,
    "emotionalRole": "recovery",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Flag if recovery/self-healing was triggered during session.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to false if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "sessionChurnRisk",
    "fieldType": "number",
    "required": false,
    "default": 0.0,
    "emotionalRole": "risk",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Predicted risk of session dropout.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 0.0 if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "sessionEmotionDelta",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "emotion",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Change in session emotionTensor over time.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing.",
      "auditTrail": true
    }
  }
] 
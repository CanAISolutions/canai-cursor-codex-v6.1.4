[
  {
    "fieldName": "impactEventId",
    "fieldType": "ULID",
    "required": true,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Primary key for ImpactEventMap records.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "createdAt",
    "fieldType": "Timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Used for audit and recovery.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "updatedAt",
    "fieldType": "Timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Tracks last update for audit.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "sessionId",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Foreign key to SessionAnalytics.sessionId for impact mapping.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not session-bound. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "userId",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "pii",
    "contextScope": "user",
    "orchestrationNotes": "User identifier for impact attribution.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if system-generated. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "eventType",
    "fieldType": "string",
    "required": true,
    "default": "custom",
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Enumerated type of impact event. Allowed values: feedback, prompt, anomaly, milestone, system, user-action, custom.",
    "codexEnforcement": {
      "required": true,
      "allowedValues": ["feedback", "prompt", "anomaly", "milestone", "system", "user-action", "custom"],
      "fallbackLogic": "Set to 'custom' if missing or invalid. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "eventContext",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Contextual metadata for the event (e.g., source, channel, metadata).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "impactScore",
    "fieldType": "number",
    "required": false,
    "default": null,
    "emotionalRole": "impact",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Impact score for the event (0.0–1.0).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not scored. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "impactDrivers",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Key-value pairs of impact driver labels and scores (e.g., {\"clarity\": 0.8, \"trust\": 0.7}).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "emotionalTags",
    "fieldType": "array",
    "required": false,
    "default": [],
    "emotionalRole": "emotion",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "List of emotional tags for the event (e.g., trust, urgency, delight, frustration).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty array if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "notes",
    "fieldType": "string",
    "required": false,
    "default": "",
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Additional notes or context for the impact event.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow empty string if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "fallbackUsed",
    "fieldType": "boolean",
    "required": false,
    "default": false,
    "emotionalRole": "resilience",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Indicates if fallback logic was triggered for this impact event.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to false if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "auditTrail",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Audit log for all changes and fallback events.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing. Audit trail required.",
      "auditTrail": true
    }
  }
] 
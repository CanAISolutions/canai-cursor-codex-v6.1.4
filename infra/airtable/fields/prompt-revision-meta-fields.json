[
  {
    "fieldName": "revisionId",
    "fieldType": "ULID",
    "required": true,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Primary key for PromptRevisionMeta records.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "createdAt",
    "fieldType": "Timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Used for audit and recovery.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "updatedAt",
    "fieldType": "Timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Tracks last update for audit.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "promptLogId",
    "fieldType": "ULID",
    "required": true,
    "default": null,
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Foreign key to PromptLogs.recordId for revision traceability.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "revisionNumber",
    "fieldType": "number",
    "required": true,
    "default": 1,
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Revision sequence number for the prompt.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to 1 if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "revisionReason",
    "fieldType": "string",
    "required": false,
    "default": "",
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Reason for the revision (e.g., user feedback, system update).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow empty string if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "revisionType",
    "fieldType": "string",
    "required": false,
    "default": "manual",
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Type of revision (e.g., manual, automated, fallback).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 'manual' if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "previousRevisionId",
    "fieldType": "ULID",
    "required": false,
    "default": null,
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Links to the previous revision for full audit trail.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if first revision. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "changes",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Object describing the changes made in this revision.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "actorId",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "ID of the user, agent, or system that performed the revision.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if system-generated. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "emotionalTags",
    "fieldType": "array",
    "required": false,
    "default": [],
    "emotionalRole": "emotion",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "List of emotional tags for the revision (e.g., trust, clarity, urgency).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty array if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "auditTrail",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Audit log for all changes and fallback events.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing. Audit trail required.",
      "auditTrail": true
    }
  }
] 
[
  {
    "fieldName": "streamId",
    "fieldType": "string",
    "required": true,
    "default": null,
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "system",
    "orchestrationNotes": "Unique identifier for each sentiment stream event.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "If missing, generate UUID and log fallback event.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "timestamp",
    "fieldType": "datetime",
    "required": true,
    "default": null,
    "emotionalRole": "timeliness",
    "dataSensitivity": "internal",
    "contextScope": "system",
    "orchestrationNotes": "Time of sentiment capture.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "If missing, use current system time and log fallback event.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "source",
    "fieldType": "string",
    "required": true,
    "default": "user",
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "system",
    "orchestrationNotes": "Source of sentiment (user, agent, system, external).",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Default to 'user' and log fallback event.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "sentimentScore",
    "fieldType": "number",
    "required": true,
    "default": 0,
    "emotionalRole": "emotional intelligence",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Numerical sentiment score (-1 to 1).",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Default to 0 and log fallback event.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "emotionalTag",
    "fieldType": "string",
    "required": false,
    "default": "neutral",
    "emotionalRole": "emotion",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Tag for emotional state (e.g., joy, frustration, trust).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Default to 'neutral' if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "contextScope",
    "fieldType": "string",
    "required": true,
    "default": "session",
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "system",
    "orchestrationNotes": "Scope of sentiment (session, user, system, external).",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Default to 'session' and log fallback event.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "codexEnforcement",
    "fieldType": "object",
    "required": true,
    "default": {},
    "emotionalRole": "audit",
    "dataSensitivity": "internal",
    "contextScope": "system",
    "orchestrationNotes": "Codex enforcement metadata for this record.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "If missing, auto-populate with default enforcement object.",
      "auditTrail": true
    }
  }
] 
[
  {
    "fieldName": "typeId",
    "fieldType": "ULID",
    "required": true,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Primary key for PromptTypeGlossary records.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "typeName",
    "fieldType": "string",
    "required": true,
    "default": null,
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Canonical name of the prompt type (e.g., businessPlan, socialContent, siteAudit).",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "description",
    "fieldType": "string",
    "required": false,
    "default": "",
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Human-readable description of the prompt type's purpose and usage.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow empty string if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "category",
    "fieldType": "string",
    "required": false,
    "default": "general",
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Category of the prompt type (e.g., business, marketing, audit, feedback).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 'general' if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "isActive",
    "fieldType": "boolean",
    "required": false,
    "default": true,
    "emotionalRole": "trust",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Whether this prompt type is currently active and available for use.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to true if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "defaultFields",
    "fieldType": "array",
    "required": false,
    "default": [],
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "List of default field names for this prompt type.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty array if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "emotionalRole",
    "fieldType": "string",
    "required": false,
    "default": "context",
    "emotionalRole": "emotion",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Primary emotional role of the prompt type (e.g., clarity, trust, context, emotion, traceability).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 'context' if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "dataSensitivity",
    "fieldType": "string",
    "required": false,
    "default": "internal",
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Data sensitivity classification (e.g., internal, pii, public).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 'internal' if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "contextScope",
    "fieldType": "string",
    "required": false,
    "default": "global",
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Scope of the prompt type (e.g., global, session, user, event, meta).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 'global' if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "glossaryVersion",
    "fieldType": "string",
    "required": false,
    "default": "v1.0.0",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Version of the glossary entry.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 'v1.0.0' if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "relatedPrompts",
    "fieldType": "array",
    "required": false,
    "default": [],
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "List of prompt log IDs or names that use this type.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty array if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "auditTrail",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Audit log for all changes and fallback events.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing. Audit trail required.",
      "auditTrail": true
    }
  }
] 
[
  {
    "fieldName": "fieldId",
    "fieldType": "ULID",
    "required": true,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Primary key for FieldGlossary records.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "fieldName",
    "fieldType": "string",
    "required": true,
    "default": null,
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Canonical name of the field.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "fieldType",
    "fieldType": "string",
    "required": true,
    "default": null,
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Type of the field (e.g., string, number, object, array, boolean, ULID, timestamp).",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "description",
    "fieldType": "string",
    "required": false,
    "default": "",
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Human-readable description of the field's purpose and usage.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow empty string if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "emotionalRole",
    "fieldType": "string",
    "required": false,
    "default": "context",
    "emotionalRole": "emotion",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Primary emotional role of the field (e.g., clarity, trust, identity, context, emotion, traceability).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 'context' if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "dataSensitivity",
    "fieldType": "string",
    "required": false,
    "default": "internal",
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Data sensitivity classification (e.g., internal, pii, public).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 'internal' if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "contextScope",
    "fieldType": "string",
    "required": false,
    "default": "global",
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Scope of the field (e.g., global, session, user, event, meta).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 'global' if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "defaultValue",
    "fieldType": "any",
    "required": false,
    "default": null,
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Default value for the field, if any.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if no default. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "isRequired",
    "fieldType": "boolean",
    "required": false,
    "default": false,
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Whether the field is required in its parent table.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to false if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "isIndexed",
    "fieldType": "boolean",
    "required": false,
    "default": false,
    "emotionalRole": "efficiency",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Whether the field is indexed for query performance.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to false if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "glossaryVersion",
    "fieldType": "string",
    "required": false,
    "default": "v1.0.0",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Version of the glossary entry.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 'v1.0.0' if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "relatedTables",
    "fieldType": "array",
    "required": false,
    "default": [],
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "List of tables where this field is used.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty array if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "auditTrail",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Audit log for all changes and fallback events.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing. Audit trail required.",
      "auditTrail": true
    }
  }
] 
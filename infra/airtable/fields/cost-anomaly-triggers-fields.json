[
  {
    "fieldName": "triggerId",
    "fieldType": "ULID",
    "required": true,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Primary key for CostAnomalyTriggers records.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "sessionId",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Foreign key to SessionAnalytics.sessionId for anomaly linkage.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not session-bound. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "userId",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "pii",
    "contextScope": "user",
    "orchestrationNotes": "User identifier for anomaly attribution.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if system-generated. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "eventTimestamp",
    "fieldType": "Timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Timestamp for when the anomaly event was detected.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "anomalyType",
    "fieldType": "string",
    "required": true,
    "default": "costSpike",
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Type of anomaly (e.g., costSpike, tokenOverrun, deliveryFailure, retryLoop).",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to 'costSpike' if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "anomalySource",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Source of the anomaly (e.g., PromptLogs, DeliveryCostLogs, externalWebhook).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if unknown. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "threshold",
    "fieldType": "number",
    "required": true,
    "default": 0.0,
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Threshold value that triggered the anomaly alert.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to 0.0 if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "observedValue",
    "fieldType": "number",
    "required": true,
    "default": 0.0,
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Actual value observed that exceeded the threshold.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to 0.0 if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "expectedValue",
    "fieldType": "number",
    "required": false,
    "default": null,
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Expected value for the metric (if available).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not available. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "promptType",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Prompt type associated with the anomaly (if applicable).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not prompt-related. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "tokenCount",
    "fieldType": "number",
    "required": false,
    "default": null,
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Token count at the time of anomaly (if applicable).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not token-related. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "fallbackCount",
    "fieldType": "number",
    "required": false,
    "default": null,
    "emotionalRole": "resilience",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Number of fallbacks triggered during the anomaly event.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not fallback-related. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "alertStatus",
    "fieldType": "string",
    "required": false,
    "default": "open",
    "emotionalRole": "trust",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Status of the alert (e.g., open, acknowledged, resolved, ignored).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 'open' if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "resolutionStatus",
    "fieldType": "string",
    "required": false,
    "default": "unresolved",
    "emotionalRole": "trust",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Resolution status of the anomaly (e.g., unresolved, resolved, escalated).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 'unresolved' if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "notes",
    "fieldType": "string",
    "required": false,
    "default": "",
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Additional notes or context for the anomaly event.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow empty string if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "auditTrail",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Audit log for all changes and fallback events.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing. Audit trail required.",
      "auditTrail": true
    }
  }
] 
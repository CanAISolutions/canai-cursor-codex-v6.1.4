[
  {
    "fieldName": "recordId",
    "fieldType": "ULID",
    "required": true,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Primary key for EmotionTensor records.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "createdAt",
    "fieldType": "Timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Used for audit and recovery.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "updatedAt",
    "fieldType": "Timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Tracks last update for audit.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "sessionId",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Links emotion tensor to a session if applicable.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not session-bound. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "userId",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "pii",
    "contextScope": "user",
    "orchestrationNotes": "Links emotion tensor to a user if available.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not user-bound. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "emotionVector",
    "fieldType": "object",
    "required": true,
    "default": {},
    "emotionalRole": "emotion",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Key-value pairs of emotion labels and scores (e.g., {\"joy\": 0.8, \"trust\": 0.7}).",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to empty object if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "emotionSource",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Source of emotion data (e.g., prompt, feedback, analytics).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if unknown. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "emotionConfidence",
    "fieldType": "number",
    "required": false,
    "default": 1.0,
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Confidence score for the emotion vector (0.0–1.0).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 1.0 if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "emotionNotes",
    "fieldType": "string",
    "required": false,
    "default": "",
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Additional notes or context for the emotion tensor.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow empty string if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "auditTrail",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Audit log for all changes and fallback events.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing. Audit trail required.",
      "auditTrail": true
    }
  }
] 
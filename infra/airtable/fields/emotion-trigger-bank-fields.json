[
  {
    "fieldName": "triggerId",
    "fieldType": "ULID",
    "required": true,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Primary key for EmotionTriggerBank records.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "emotionTag",
    "fieldType": "string",
    "required": true,
    "default": null,
    "emotionalRole": "emotion",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Emotion label or tag (e.g., trust, delight, frustration, anticipation).",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "triggerType",
    "fieldType": "string",
    "required": true,
    "default": "system",
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Type of trigger (e.g., system, user, feedback, anomaly, milestone).",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to 'system' if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "triggerSource",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Source of the trigger (e.g., PromptLogs, FeedbackLogs, analytics, external).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if unknown. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "triggerContext",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Additional context for the trigger (e.g., event details, user state).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "triggeredAt",
    "fieldType": "Timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Timestamp for when the emotion trigger was detected.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "triggerStrength",
    "fieldType": "number",
    "required": false,
    "default": 1.0,
    "emotionalRole": "impact",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Strength or confidence of the trigger (0.0–1.0).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 1.0 if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "notes",
    "fieldType": "string",
    "required": false,
    "default": "",
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Additional notes or context for the emotion trigger event.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow empty string if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "auditTrail",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Audit log for all changes and fallback events.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing. Audit trail required.",
      "auditTrail": true
    }
  }
] 
[
  {
    "fieldName": "signalId",
    "fieldType": "ULID",
    "required": true,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Primary key for TrustSignals records.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "createdAt",
    "fieldType": "Timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Used for audit and recovery.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "updatedAt",
    "fieldType": "Timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Tracks last update for audit.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "sessionId",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Foreign key to SessionAnalytics.sessionId for trust linkage.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not session-bound. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "userId",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "pii",
    "contextScope": "user",
    "orchestrationNotes": "User identifier for trust attribution.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if system-generated. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "trustScore",
    "fieldType": "number",
    "required": true,
    "default": 0.5,
    "emotionalRole": "trust",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Trust score (0.0–1.0) for the session/user/event.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to 0.5 if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "signalType",
    "fieldType": "string",
    "required": true,
    "default": "generic",
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Type of trust signal (e.g., feedback, system, referral, anomaly).",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to 'generic' if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "signalSource",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Source of the trust signal (e.g., PromptLogs, FeedbackLogs, external).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if unknown. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "context",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Additional context for the trust signal.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "emotionalTags",
    "fieldType": "array",
    "required": false,
    "default": [],
    "emotionalRole": "emotion",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "List of emotional tags for the trust signal (e.g., trust, clarity, urgency).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty array if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "auditTrail",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Audit log for all changes and fallback events.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing. Audit trail required.",
      "auditTrail": true
    }
  }
] 
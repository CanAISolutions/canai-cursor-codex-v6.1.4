[
  {
    "fieldName": "recoveryId",
    "fieldType": "ULID",
    "required": true,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Primary key for SessionRecoveryMap records.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "sessionId",
    "fieldType": "string",
    "required": true,
    "default": null,
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Foreign key to SessionAnalytics.sessionId for recovery linkage.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "recoveryType",
    "fieldType": "string",
    "required": true,
    "default": "auto",
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Type of recovery (e.g., auto, manual, fallback, escalation).",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to 'auto' if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "triggerEvent",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Event that triggered the recovery (e.g., anomaly, user-action, timeout).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not event-triggered. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "recoveryStatus",
    "fieldType": "string",
    "required": true,
    "default": "initiated",
    "emotionalRole": "resilience",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Status of the recovery (e.g., initiated, in-progress, completed, failed).",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to 'initiated' if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "recoveryTimestamp",
    "fieldType": "Timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Timestamp for when the recovery was initiated.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "resolvedTimestamp",
    "fieldType": "Timestamp",
    "required": false,
    "default": null,
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Timestamp for when the recovery was resolved (if applicable).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not resolved. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "recoveryOutcome",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "outcome",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Outcome of the recovery (e.g., success, partial, failed, escalated).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not yet resolved. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "notes",
    "fieldType": "string",
    "required": false,
    "default": "",
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Additional notes or context for the recovery event.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow empty string if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "auditTrail",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Audit log for all changes and fallback events.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing. Audit trail required.",
      "auditTrail": true
    }
  }
] 
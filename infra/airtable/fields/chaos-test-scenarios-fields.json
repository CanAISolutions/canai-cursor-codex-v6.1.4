[
  {
    "fieldName": "scenarioId",
    "fieldType": "string",
    "required": true,
    "default": null,
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "system",
    "orchestrationNotes": "Unique identifier for each chaos scenario.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "If missing, generate UUID and log fallback event.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "description",
    "fieldType": "string",
    "required": true,
    "default": "",
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "system",
    "orchestrationNotes": "Describes the chaos scenario and its intent.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "If missing, flag for operator review.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "triggerType",
    "fieldType": "enum",
    "required": true,
    "default": "manual",
    "emotionalRole": "resilience",
    "dataSensitivity": "internal",
    "contextScope": "system",
    "orchestrationNotes": "Type of trigger (manual, scheduled, random, event-driven).",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Default to 'manual' and log fallback event.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "expectedOutcome",
    "fieldType": "string",
    "required": true,
    "default": "System recovers gracefully",
    "emotionalRole": "trust",
    "dataSensitivity": "internal",
    "contextScope": "system",
    "orchestrationNotes": "Expected system behavior during/after chaos event.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Default to 'System recovers gracefully' and log fallback event.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "fallbackPath",
    "fieldType": "string",
    "required": false,
    "default": "Standard fallback",
    "emotionalRole": "resilience",
    "dataSensitivity": "internal",
    "contextScope": "system",
    "orchestrationNotes": "Fallback logic or path if scenario fails.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Default to 'Standard fallback' if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "emotionalImpact",
    "fieldType": "string",
    "required": false,
    "default": "neutral",
    "emotionalRole": "emotional intelligence",
    "dataSensitivity": "internal",
    "contextScope": "system",
    "orchestrationNotes": "Expected or observed emotional impact of the scenario.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Default to 'neutral' if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "codexEnforcement",
    "fieldType": "object",
    "required": true,
    "default": {},
    "emotionalRole": "audit",
    "dataSensitivity": "internal",
    "contextScope": "system",
    "orchestrationNotes": "Codex enforcement metadata for this record.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "If missing, auto-populate with default enforcement object.",
      "auditTrail": true
    }
  }
] 
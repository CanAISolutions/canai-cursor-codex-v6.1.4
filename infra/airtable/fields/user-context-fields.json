[
  {
    "fieldName": "recordId",
    "fieldType": "ulid",
    "required": true,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Primary key for UserContext. Referenced by all related tables.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "createdAt",
    "fieldType": "timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Used for audit and recovery.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "updatedAt",
    "fieldType": "timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Tracks last update for audit.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "userId",
    "fieldType": "string",
    "required": true,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "pii",
    "contextScope": "user",
    "orchestrationNotes": "User identifier for context binding. Foreign key to user system.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "personaProfile",
    "fieldType": "object",
    "required": true,
    "default": {},
    "emotionalRole": "persona",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Persona archetype and profile (e.g., 'visionary', 'operator', 'creator'). Drives personalization and emotional resonance.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Log as empty object and flag for review.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "cultureVector",
    "fieldType": "array",
    "required": false,
    "default": [],
    "emotionalRole": "culture",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Cultural tags (e.g., ['EMEA', 'startup', 'risk-tolerant']). Used for adaptive UX and analytics.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty array if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "emotionalTraits",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "emotion",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Key emotional traits (e.g., empathy, ambition, resilience). Used for emotional memory and trust-building.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "cognitiveStyle",
    "fieldType": "string",
    "required": false,
    "default": "balanced",
    "emotionalRole": "cognition",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Cognitive style (e.g., 'analytical', 'creative', 'balanced'). Used for adaptive prompt and UX flows.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 'balanced' if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "contextVector",
    "fieldType": "array",
    "required": false,
    "default": [],
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Contextual tags (e.g., ['novice', 'bold']). Used for session adaptation and analytics.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty array if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "trustLevel",
    "fieldType": "number",
    "required": false,
    "default": 0.5,
    "emotionalRole": "trust",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Trust score for the user (0.0–1.0). Used for fallback, escalation, and analytics.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 0.5 if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "memoryTokens",
    "fieldType": "array",
    "required": false,
    "default": [],
    "emotionalRole": "memory",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Tokens or keys for memory recall and session continuity.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty array if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "auditTrail",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Audit log for all changes and fallback events.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing.",
      "auditTrail": true
    }
  }
] 
[
  {
    "scenarioId": "chaos-001",
    "description": "Simulate API latency spike during peak hours.",
    "triggerType": "scheduled",
    "expectedOutcome": "System recovers gracefully",
    "fallbackPath": "Retry with exponential backoff",
    "emotionalImpact": "stress",
    "codexEnforcement": { "required": true, "fallbackLogic": "If missing, generate UUID and log fallback event.", "auditTrail": true }
  },
  {
    "scenarioId": "chaos-002",
    "description": "Randomly drop database connection.",
    "triggerType": "random",
    "expectedOutcome": "System switches to read-only mode",
    "fallbackPath": "Activate cache layer",
    "emotionalImpact": "uncertainty",
    "codexEnforcement": { "required": true, "fallbackLogic": "If missing, generate UUID and log fallback event.", "auditTrail": true }
  },
  {
    "scenarioId": "chaos-003",
    "description": "Corrupt session data for a single user.",
    "triggerType": "manual",
    "expectedOutcome": "Session auto-recovers with minimal data loss",
    "fallbackPath": "Restore from backup",
    "emotionalImpact": "concern",
    "codexEnforcement": { "required": true, "fallbackLogic": "If missing, generate UUID and log fallback event.", "auditTrail": true }
  },
  {
    "scenarioId": "chaos-004",
    "description": "Trigger with missing fallbackPath (test fallback logic).",
    "triggerType": "event-driven",
    "expectedOutcome": "System logs fallback event and continues operation",
    "emotionalImpact": "neutral",
    "codexEnforcement": { "required": true, "fallbackLogic": "If missing, generate UUID and log fallback event.", "auditTrail": true }
  }
] 
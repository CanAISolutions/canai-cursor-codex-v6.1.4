[
  {
    "scenarioId": "chaos-test-001",
    "description": "Test missing triggerType (should default to 'manual').",
    "expectedOutcome": "System recovers gracefully",
    "fallbackPath": "Standard fallback",
    "emotionalImpact": "neutral",
    "codexEnforcement": { "required": true, "fallbackLogic": "Default to 'manual' and log fallback event.", "auditTrail": true }
  },
  {
    "scenarioId": "chaos-test-002",
    "description": "Test missing expectedOutcome (should default and log fallback).",
    "triggerType": "manual",
    "fallbackPath": "Standard fallback",
    "emotionalImpact": "neutral",
    "codexEnforcement": { "required": true, "fallbackLogic": "Default to 'System recovers gracefully' and log fallback event.", "auditTrail": true }
  },
  {
    "scenarioId": "chaos-test-003",
    "description": "Test missing fallbackPath and emotionalImpact (should default and log fallback).",
    "triggerType": "scheduled",
    "expectedOutcome": "System recovers gracefully",
    "codexEnforcement": { "required": true, "fallbackLogic": "Default to 'Standard fallback' and 'neutral' if missing.", "auditTrail": true }
  },
  {
    "scenarioId": "chaos-test-004",
    "description": "Test missing codexEnforcement (should auto-populate and log fallback).",
    "triggerType": "random",
    "expectedOutcome": "System recovers gracefully",
    "fallbackPath": "Standard fallback",
    "emotionalImpact": "neutral"
  }
] 
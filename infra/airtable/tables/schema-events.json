{
  "tableName": "SchemaEvents",
  "description": "Logs all schema changes, drifts, rollbacks, and related events for traceability and audit.",
  "version": "v1.0.0",
  "fieldsFile": "../fields/schema-events-fields.json",
  "schemaFile": "../schemas/schema-events-schema.md",
  "mockFile": "./schema-events.mock.json",
  "testFile": "./schema-events.test.json",
  "codexEnforcement": {
    "auditTrail": true,
    "fallbackLogic": "If table is unavailable, log to FallbackStore and emit SchemaEvents.",
    "versioned": true
  }
} 
{
    // @codex-purpose: "Define required GPT output structure per promptType to validate snapshot integrity and rendering compatibility"
    // @codex-system: "Prompt Fulfillment QA Layer (Copilot Snapshot Diff + Output Renderer Safety)"
    // @codex-critical: "Prevents GPT hallucinated output fields, silent render breakage, and unscored output misalignments"
    // @codex-verified: "v1.4.2"
  
    "outputShape": {
      "business_plan": {
        "requiredFields": ["executiveSummary", "goToMarket", "revenueModel", "teamSummary"],
        "optionalFields": ["financialProjection", "uniqueAdvantage"]
      },
      "email_campaign": {
        "requiredFields": ["subjectLine", "bodyText", "cta"],
        "optionalFields": ["previewText", "urgencyTag"]
      },
      "site_audit": {
        "requiredFields": ["problemAreas", "quickFixes", "priorityRecommendations"],
        "optionalFields": ["clarityScore", "estimatedImpact"]
      },
      "ai_blueprint": {
        "requiredFields": ["strategyOverview", "recommendedModels", "integrationPlan"],
        "optionalFields": ["riskAnalysis", "rolloutTimeline"]
      },
      "reverse_strategy": {
        "requiredFields": ["milestones", "workBackPlan", "keyDependencies"],
        "optionalFields": ["fallbackPlan"]
      },
      "ai_brand_identity": {
        "requiredFields": ["toneProfile", "valuePillars", "messagingExamples"],
        "optionalFields": ["colorSuggestions", "taglineIdeas"]
      },
      "social_content": {
        "requiredFields": ["platform", "postText", "cta"],
        "optionalFields": ["hashtags", "emojiEnhancement"]
      }
    },
  
    "_meta": {
      "description": "Enforces output consistency per promptType, protects UI rendering, snapshot diffing, and Copilot validation. Prevents silent failure from malformed outputs.",
      "assert": "matchOutputStructure",
      "guardrailFor": "PromptType output validation and downstream rendering logic",
      "triggerField": "requiredFields",
      "snapshotCompatible": true,
      "lastVerifiedAgainst": "v1.4.2"
    }
  }
  
// @codex-purpose: Simulate an incoming event with no type field to test null-routing and schema enforcement
// @codex-system: event routing + fallback handler
// @codex-critical: Prevents undefined behavior and silent automation misfires due to missing classifications
// @codex-verified: v1.4.2

{
  // 🚫 No "type" field present
  "source": "webhook",
  "payload": {
    "userId": "ghost-001",
    "origin": "external-test-trigger"
  },
  "_meta": {
    "description": "This payload simulates a webhook event that is missing the required 'type' field. It validates the system's ability to catch undefined classifications and route to fallback logic without triggering an error or silent failure.",
    "assert": "matchErrorStructure",
    "triggerField": "missing type",
    "guardrailFor": "event-router.ts + fallback classification",
    "routeTo": "event-router.ts",
    "snapshotCompatible": true,
    "lastVerifiedAgainst": "v1.4.2"
  }
}

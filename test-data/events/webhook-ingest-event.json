// @codex-purpose: Simulate a well-formed inbound webhook event from an external system (e.g. Make.com)
// @codex-system: webhook ingest router
// @codex-critical: Prevents unvalidated third-party triggers from corrupting routing logic or prompt session attribution
// @codex-verified: v1.4.2

{
  "type": "webhook-ingest",
  "source": "external",
  "payload": {
    "eventId": "ext-webhook-4571",
    "userId": "user_555",
    "eventLabel": "output-ready",
    "timestamp": "2024-05-01T13:20:00Z",
    "origin": "make.com"
  },
  "_meta": {
    "description": "Simulates a real inbound webhook sent by an external automation system (like Make). Ensures event-router ingests properly, attributes correctly, and does not silently fail under incomplete or unexpected payloads.",
    "assert": "matchEventShape",
    "triggerField": "eventId",
    "guardrailFor": "event-router.ts",
    "routeTo": "event-router.ts",
    "snapshotCompatible": true,
    "lastVerifiedAgainst": "v1.4.2"
  }
}

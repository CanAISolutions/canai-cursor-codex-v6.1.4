// @codex-purpose: Simulate structurally invalid input missing required prompt fields
// @codex-system: API schema validator
// @codex-critical: Ensures malformed payloads are caught before downstream processing
// @codex-verified: v1.4.2

{
  // ❌ No 'promptType' field present — this is the schema-breaking trigger
  "_meta": {
    "description": "This file represents a malformed GPT input payload — specifically one missing the required 'promptType'. It tests the system's ability to fail fast and clearly when required fields are missing at the top level.",
    "expectedFailure": "MalformedRequestError",
    "triggerField": "missing promptType",
    "snapshotCompatible": true,
    "lastVerifiedAgainst": "v1.4.2"
  }
}

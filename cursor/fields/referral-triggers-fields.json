[
  {
    "fieldName": "recordId",
    "fieldType": "ULID",
    "required": true,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Primary key for ReferralTriggers records.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "createdAt",
    "fieldType": "Timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Timestamp for record creation.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "updatedAt",
    "fieldType": "Timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Timestamp for last update.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "referrerUserId",
    "fieldType": "string",
    "required": true,
    "default": null,
    "emotionalRole": "trust",
    "dataSensitivity": "pii",
    "contextScope": "user",
    "orchestrationNotes": "User initiating the referral. Foreign key to UserContext.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "referredUserId",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "growth",
    "dataSensitivity": "pii",
    "contextScope": "user",
    "orchestrationNotes": "User being referred (may be null if not yet registered).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not registered. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "triggerEvent",
    "fieldType": "string",
    "required": true,
    "default": "referralSent",
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Event type (e.g., referralSent, referralClaimed, rewardGranted, reactivationTriggered, shareMedium).",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to 'referralSent' if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "rewardType",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "incentive",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Type of reward granted (e.g., credit, featureUnlock, badge).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not rewarded. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "referralStatus",
    "fieldType": "string",
    "required": false,
    "default": "pending",
    "emotionalRole": "status",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Status of the referral (e.g., pending, converted, expired, failed).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 'pending' if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "referralConversion",
    "fieldType": "boolean",
    "required": false,
    "default": false,
    "emotionalRole": "outcome",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Indicates if referral led to conversion.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to false if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "campaignTag",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Tag for campaign attribution (e.g., 'spring2025', 'reactivationQ2').",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not tagged. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "shareMedium",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "channel",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Medium used for sharing (e.g., email, sms, social, link).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not specified. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "likelySharerType",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "insight",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Derived: likely type of sharer (e.g., influencer, powerUser, casual).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not derived. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "referralInfluenceScore",
    "fieldType": "number",
    "required": false,
    "default": 0,
    "emotionalRole": "impact",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Derived: score estimating influence of this referral.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 0 if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "reactivationLag",
    "fieldType": "number",
    "required": false,
    "default": null,
    "emotionalRole": "timing",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Derived: time (in hours) between referral and reactivation event.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not applicable. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "auditTrail",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "session",
    "orchestrationNotes": "Audit log for all changes and fallback events.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing. Audit trail required.",
      "auditTrail": true
    }
  }
] 
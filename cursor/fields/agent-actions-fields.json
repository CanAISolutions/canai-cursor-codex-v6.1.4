[
  {
    "fieldName": "actionId",
    "fieldType": "ULID",
    "required": true,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Primary key for AgentActions records.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "timestamp",
    "fieldType": "Timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Timestamp for action creation.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "agentName",
    "fieldType": "string",
    "required": true,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Name of the agent performing the action.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "triggerType",
    "fieldType": "string",
    "required": true,
    "default": null,
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Type of trigger (e.g., user, system, schedule, feedback, self-heal).",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "affectedTable",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Name of the table affected by the action.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not table-specific. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "actionDescription",
    "fieldType": "string",
    "required": true,
    "default": null,
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Description of the action performed by the agent.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "fallbackUsed",
    "fieldType": "boolean",
    "required": false,
    "default": false,
    "emotionalRole": "resilience",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Indicates if fallback logic was triggered for this action.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to false if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "revisionGenerated",
    "fieldType": "boolean",
    "required": false,
    "default": false,
    "emotionalRole": "improvement",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Flag if the action generated a revision (e.g., prompt revision, schema update).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to false if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "codexRuleApplied",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "trust",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Codex rule or contract applied during the action.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not rule-based. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "outputDelta",
    "fieldType": "number",
    "required": false,
    "default": 0.0,
    "emotionalRole": "impact",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Delta or change in output as a result of the action.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 0.0 if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "confidenceScore",
    "fieldType": "number",
    "required": false,
    "default": 1.0,
    "emotionalRole": "confidence",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Confidence score for the action (0.0–1.0).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 1.0 if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "autonomyLevel",
    "fieldType": "string",
    "required": false,
    "default": "operator",
    "emotionalRole": "autonomy",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Level of agent autonomy (e.g., operator, co-pilot, autonomous).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 'operator' if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "emotionalImpactTag",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "emotion",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Tag for emotional impact (e.g., trust, delight, frustration, confidence).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not tagged. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "agentPersona",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "persona",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Persona or archetype of the agent (e.g., Sentinel, Operator, Guide).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not persona-specific. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "resonanceShift",
    "fieldType": "number",
    "required": false,
    "default": 0.0,
    "emotionalRole": "empathy",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Change in resonance or emotional alignment as a result of the action.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 0.0 if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "selfHealFlag",
    "fieldType": "boolean",
    "required": false,
    "default": false,
    "emotionalRole": "resilience",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Flag if the action was part of a self-healing or recovery process.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to false if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "auditTrail",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Audit log for all changes and fallback events.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing. Audit trail required.",
      "auditTrail": true
    }
  }
] 
[
  {
    "fieldName": "recordId",
    "fieldType": "ULID",
    "required": true,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Primary key for UserContext records.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "createdAt",
    "fieldType": "Timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Timestamp for record creation.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "updatedAt",
    "fieldType": "Timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Timestamp for last update.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "userId",
    "fieldType": "string",
    "required": true,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "pii",
    "contextScope": "user",
    "orchestrationNotes": "User identifier for context binding. Foreign key to user system.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "personaProfile",
    "fieldType": "object",
    "required": true,
    "default": {},
    "emotionalRole": "persona",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Persona archetype and profile (e.g., 'visionary', 'operator', 'creator'). Drives personalization and emotional resonance.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Log as empty object and flag for review.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "emotionalTraits",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "emotion",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Key emotional traits (e.g., empathy, ambition, resilience). Used for emotional memory and trust-building.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "emotionalTone",
    "fieldType": "string",
    "required": false,
    "default": "neutral",
    "emotionalRole": "emotion",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Dominant emotional tone (e.g., optimistic, anxious, neutral).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 'neutral' if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "intentConfidence",
    "fieldType": "number",
    "required": false,
    "default": 0.5,
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Confidence score for inferred user intent (0.0–1.0).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 0.5 if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "engagementCluster",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "engagement",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Cluster label for engagement segmentation (e.g., explorer, power, dormant).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not clustered.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "lastAction",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Most recent user action or event.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if unknown.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "deviceType",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Device type (e.g., mobile, desktop, tablet).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if unknown.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "timezone",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "User's timezone (e.g., 'America/New_York').",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if unknown.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "trustTier",
    "fieldType": "string",
    "required": false,
    "default": "standard",
    "emotionalRole": "trust",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Tiered trust level (e.g., standard, elevated, restricted).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 'standard' if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "personalizationFlags",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "personalization",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Flags for feature access, A/B tests, or UX variants.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "cultureVector",
    "fieldType": "array",
    "required": false,
    "default": [],
    "emotionalRole": "culture",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Cultural tags (e.g., ['EMEA', 'startup', 'risk-tolerant']).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty array if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "cognitiveStyle",
    "fieldType": "string",
    "required": false,
    "default": "balanced",
    "emotionalRole": "cognition",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Cognitive style (e.g., 'analytical', 'creative', 'balanced').",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 'balanced' if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "contextVector",
    "fieldType": "array",
    "required": false,
    "default": [],
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Contextual tags (e.g., ['novice', 'bold']).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty array if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "trustLevel",
    "fieldType": "number",
    "required": false,
    "default": 0.5,
    "emotionalRole": "trust",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Trust score for the user (0.0–1.0).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 0.5 if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "memoryTokens",
    "fieldType": "array",
    "required": false,
    "default": [],
    "emotionalRole": "memory",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Tokens or keys for memory recall and session continuity.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty array if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "likelyDropoffStep",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "risk",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Derived: most likely step for user dropoff.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not available.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "preferredFulfillmentSpeed",
    "fieldType": "string",
    "required": false,
    "default": "standard",
    "emotionalRole": "preference",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Derived: preferred speed for fulfillment (e.g., instant, standard, delayed).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to 'standard' if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "reactivationOpportunity",
    "fieldType": "boolean",
    "required": false,
    "default": false,
    "emotionalRole": "opportunity",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Derived: flag if user is a candidate for reactivation.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to false if missing.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "auditTrail",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "user",
    "orchestrationNotes": "Audit log for all changes and fallback events.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing. Audit trail required.",
      "auditTrail": true
    }
  }
] 
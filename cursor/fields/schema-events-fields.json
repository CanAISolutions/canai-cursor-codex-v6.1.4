[
  {
    "fieldName": "eventId",
    "fieldType": "ULID",
    "required": true,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Primary key for SchemaEvents records.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "timestamp",
    "fieldType": "Timestamp",
    "required": true,
    "default": "now()",
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "global",
    "orchestrationNotes": "Timestamp for event creation.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Set to now() if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "tableName",
    "fieldType": "string",
    "required": true,
    "default": null,
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Name of the table affected by the event.",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "changeType",
    "fieldType": "string",
    "required": true,
    "default": null,
    "emotionalRole": "clarity",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Type of schema event (e.g., create, update, delete, drift, rollback).",
    "codexEnforcement": {
      "required": true,
      "fallbackLogic": "Block record creation if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "fieldChanged",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Name of the field affected by the event (if applicable).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not field-specific. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "previousState",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Previous state of the field/table before the event.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "newState",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "New state of the field/table after the event.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "triggerSource",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Source of the event trigger (e.g., agent, user, system, automation).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if unknown. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "codexSignature",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "trust",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Codex signature hash for event verification.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not signed. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "schemaVersion",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Schema version at the time of the event.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not versioned. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "initiatedBy",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "identity",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "ID of the user, agent, or system that initiated the event.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if system-generated. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "eventComment",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "context",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Additional comment or context for the event.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not commented. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "riskLevel",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "risk",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Risk level associated with the event (e.g., high, medium, low).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not assessed. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "impactSurface",
    "fieldType": "string",
    "required": false,
    "default": null,
    "emotionalRole": "impact",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Surface area or scope of impact (e.g., table, field, system).",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Allow null if not assessed. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "codexViolationFlag",
    "fieldType": "boolean",
    "required": false,
    "default": false,
    "emotionalRole": "trust",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Flag if event represents a Codex violation.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to false if missing. Audit trail required.",
      "auditTrail": true
    }
  },
  {
    "fieldName": "auditTrail",
    "fieldType": "object",
    "required": false,
    "default": {},
    "emotionalRole": "traceability",
    "dataSensitivity": "internal",
    "contextScope": "event",
    "orchestrationNotes": "Audit log for all changes and fallback events.",
    "codexEnforcement": {
      "required": false,
      "fallbackLogic": "Set to empty object if missing. Audit trail required.",
      "auditTrail": true
    }
  }
] 
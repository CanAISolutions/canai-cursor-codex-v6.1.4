39. **Hypothesis Fuzzing** – Pre‑merge agent generates property‑based tests covering edge inputs; PR blocked if omitted.
40. **Mutation Guard** – Mutation testing must kill ≥ 90 % of mutations; else CI fails.
41. **Snapshot Semantic Replay** – Historical gold‑sessions replay must achieve cosine sim ≥ 0.90; else block.
42. **Invariant Assertions** – Financial fields (e.g., margin) must respect invariant ranges; violations fail CI.
43. **Auto‑Rollback Hook** – Deploy script must include automatic rollback on 2 consecutive 5xx within 5 min.
44. **Perf Baseline Drift Alert** – Avg latency drift > 10 % across 1 h triggers PagerDuty event to agent.
45. **Founders’ Override Decay** – Any SHA in `allow-list.txt` auto‑expires after 24 h and re‑runs CI.
46. **Continuous Rule Learning** – Monthly agent proposes rule additions based on CI failure analytics; founders approve.
